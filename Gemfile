gem "rack"

gem "rspec", :only => :testing

gem "mocha", :only => :testing

gem "activesupport", :only => :testing

gem "cucumber", :only => :testing

gem "fletcherm-culerity", :only => :testing

disable_system_gems
